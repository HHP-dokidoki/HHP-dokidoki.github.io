<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>[数据结构学习笔记]异或链表</title>
    <url>/posts/34242/</url>
    <content><![CDATA[<h1>异或链表</h1>
<hr>
<h2 id="定义">定义</h2>
<p>  异或链表（XOR Linked List）本质上是双向链表，但它利用按位异或的值，仅使用一个指针的内存大小便可以实现双向链表的功能。</p>
<hr>
<h2 id="实现原理">实现原理</h2>
<p>  异或链表的核心原理是异或运算(<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mo>⊕</mo></mrow><annotation encoding="application/x-tex">\oplus</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.6667em;vertical-align:-0.0833em;"></span><span class="mord">⊕</span></span></span></span>)的自反性，即 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>A</mi><mo>⊕</mo><mi>A</mi><mo>=</mo><mn>0</mn></mrow><annotation encoding="application/x-tex"> A \oplus A = 0 </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7667em;vertical-align:-0.0833em;"></span><span class="mord mathnormal">A</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal">A</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span></span></span></span>，<span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>A</mi><mo>⊕</mo><mn>0</mn><mo>=</mo><mi>A</mi></mrow><annotation encoding="application/x-tex"> A \oplus 0 = A </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7667em;vertical-align:-0.0833em;"></span><span class="mord mathnormal">A</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6833em;"></span><span class="mord mathnormal">A</span></span></span></span>。使用异或链表时，我们在结构<code>Node</code>中定义<code>xorp</code>为 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>p</mi><mi>r</mi><mi>e</mi><mo>⊕</mo><mi>n</mi><mi>e</mi><mi>x</mi><mi>t</mi></mrow><annotation encoding="application/x-tex"> pre \oplus next </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7778em;vertical-align:-0.1944em;"></span><span class="mord mathnormal">p</span><span class="mord mathnormal">re</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.6151em;"></span><span class="mord mathnormal">n</span><span class="mord mathnormal">e</span><span class="mord mathnormal">x</span><span class="mord mathnormal">t</span></span></span></span> ，即前后两个节点地址的按位异或，正向遍历时只需要用前驱的地址与<code>xorp</code>进行异或运算即可得到后继的地址( <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>x</mi><mi>o</mi><mi>r</mi><mi>p</mi><mo>⊕</mo><mi>p</mi><mi>r</mi><mi>e</mi><mo>=</mo><mi>p</mi><mi>r</mi><mi>e</mi><mo>⊕</mo><mi>n</mi><mi>e</mi><mi>x</mi><mi>t</mi><mo>⊕</mo><mi>p</mi><mi>r</mi><mi>e</mi><mo>=</mo><mi>n</mi><mi>e</mi><mi>x</mi><mi>t</mi><mo>⊕</mo><mn>0</mn><mo>=</mo><mi>n</mi><mi>e</mi><mi>x</mi><mi>t</mi></mrow><annotation encoding="application/x-tex"> xorp \oplus pre = pre \oplus next \oplus pre = next \oplus 0 = next </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.7778em;vertical-align:-0.1944em;"></span><span class="mord mathnormal">x</span><span class="mord mathnormal" style="margin-right:0.02778em;">or</span><span class="mord mathnormal">p</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.625em;vertical-align:-0.1944em;"></span><span class="mord mathnormal">p</span><span class="mord mathnormal">re</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.7778em;vertical-align:-0.1944em;"></span><span class="mord mathnormal">p</span><span class="mord mathnormal">re</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.6984em;vertical-align:-0.0833em;"></span><span class="mord mathnormal">n</span><span class="mord mathnormal">e</span><span class="mord mathnormal">x</span><span class="mord mathnormal">t</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.625em;vertical-align:-0.1944em;"></span><span class="mord mathnormal">p</span><span class="mord mathnormal">re</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6984em;vertical-align:-0.0833em;"></span><span class="mord mathnormal">n</span><span class="mord mathnormal">e</span><span class="mord mathnormal">x</span><span class="mord mathnormal">t</span><span class="mspace" style="margin-right:0.2222em;"></span><span class="mbin">⊕</span><span class="mspace" style="margin-right:0.2222em;"></span></span><span class="base"><span class="strut" style="height:0.6444em;"></span><span class="mord">0</span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:0.6151em;"></span><span class="mord mathnormal">n</span><span class="mord mathnormal">e</span><span class="mord mathnormal">x</span><span class="mord mathnormal">t</span></span></span></span> )，反向遍历同理。</p>
<hr>
<h2 id="实现方法">实现方法</h2>
<h3 id="结构体定义">结构体定义</h3>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">node</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">node</span>* <span class="title">xorp</span>;</span> <span class="comment">// xorp = pre XOR next;</span></span><br><span class="line">&#125;Node;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">list</span>&#123;</span></span><br><span class="line">    Node* head;</span><br><span class="line">    Node* tail;</span><br><span class="line">&#125;Linked_List;         <span class="comment">// 存储列表的首尾节点地址</span></span><br></pre></td></tr></table></figure>
<hr>
<h3 id="辅助函数">辅助函数</h3>
<p>指针异或运算</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Node* <span class="title function_">xor</span><span class="params">(Node* a, Node* b)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 将指针转换为uintptr_t进行异或运算，再转回Node指针</span></span><br><span class="line">    <span class="comment">// uintptr_t 为 stdint.h 库中提供的能存储指针的数据类型</span></span><br><span class="line">    <span class="keyword">return</span> (Node*)((<span class="type">uintptr_t</span>)a ^ (<span class="type">uintptr_t</span>)b); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>创建新节点，分配内存并初始化value，xorp初始化为NULL</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Node* <span class="title function_">create_node</span><span class="params">(<span class="type">int</span> value)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node* node = <span class="literal">NULL</span>;</span><br><span class="line">    node = (Node*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(Node));</span><br><span class="line">    <span class="keyword">if</span> (node == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;malloc failed&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    node-&gt;value = value;</span><br><span class="line">    node-&gt;xorp  = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<p>从简单入手，先实现不需遍历链表的首尾插入与删除</p>
<h3 id="首插-尾插">首插/尾插</h3>
<h4 id="首插">首插</h4>
<p>首插主要分为三步进行：</p>
<ol>
<li>更新原表头<code>xorp</code></li>
<li>更新新表头<code>xorp</code></li>
<li>更新链表表头</li>
</ol>
<p>具体实现如下：<br>
时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mn>1</mn><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(1) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord">1</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">push_front</span><span class="params">(Linked_List* linked_list, <span class="type">int</span> value)</span> </span><br><span class="line">&#123;</span><br><span class="line">    Node* new_node = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    new_node = create_node(value);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        linked_list-&gt;tail       = new_node;</span><br><span class="line">        linked_list-&gt;head       = new_node;</span><br><span class="line">        linked_list-&gt;tail-&gt;xorp = <span class="literal">NULL</span>;</span><br><span class="line">        linked_list-&gt;head-&gt;xorp = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    linked_list-&gt;head-&gt;xorp = xor(new_node, linked_list-&gt;head-&gt;xorp);</span><br><span class="line">    new_node-&gt;xorp        = linked_list-&gt;head;</span><br><span class="line">    linked_list-&gt;head       = new_node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  当首插时链表为空，则直接将链表的首尾节点设置为要插入的节点的地址。此时首尾节点的前驱后继皆为<code>NULL</code>，而 <code>NULL XOR NULL = NULL</code> ，所以再将首尾节点的<code>xorp</code>均设置为<code>NULL</code>。</p>
<p>  当首插时链表不为空，则先更新链表原表头的<code>xorp</code>：</p>
<ol>
<li>
<p>设原表头的后继的地址为<code>next</code>，插入新节点前，表头的<code>xorp</code>为 <code>NULL next</code> ，插入新节点后，原表头的前驱变为<code>new_node</code>，后继不变，所以我们要将原表头的<code>xorp</code>更新为 <code>new_node next</code>，只需将原本的<code>xorp</code>与<code>new_node</code>进行一次异或运算即可;</p>
</li>
<li>
<p>设置新节点的<code>xorp</code>为 <code>NULL XOR linked_list-&gt;head</code> 即可;</p>
</li>
<li>
<p>将链表表头更新为 <code>new_node</code>。</p>
</li>
</ol>
<h4 id="尾插">尾插</h4>
<p>尾插同理，这里直接放代码：<br>
时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mn>1</mn><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(1) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord">1</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">push_back</span><span class="params">(Linked_List* linked_list, <span class="type">int</span> value)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node *new_node = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    new_node = create_node(value);</span><br><span class="line">    <span class="comment">/* </span></span><br><span class="line"><span class="comment">       如果链表为空，新节点既是头节点也是尾节点 </span></span><br><span class="line"><span class="comment">       xorp 设为 NULL </span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;tail == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        linked_list-&gt;tail       = new_node;</span><br><span class="line">        linked_list-&gt;head       = new_node;</span><br><span class="line">        linked_list-&gt;tail-&gt;xorp = <span class="literal">NULL</span>;</span><br><span class="line">        linked_list-&gt;head-&gt;xorp = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">       如果链表不为空：</span></span><br><span class="line"><span class="comment">        1. 更新旧尾节点的xorp: xor(linked_list-&gt;tail-&gt;xorp, new_node) 相当于</span></span><br><span class="line"><span class="comment">          (原倒数第二个节点 XOR NULL) XOR new_node</span></span><br><span class="line"><span class="comment">        2. 新节点的xorp应为 (旧尾节点 XOR NULL = 旧尾节点) </span></span><br><span class="line"><span class="comment">        3. 更新链表尾指针</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    linked_list-&gt;tail-&gt;xorp = xor(new_node, linked_list-&gt;tail-&gt;xorp);</span><br><span class="line">    new_node-&gt;xorp        = linked_list-&gt;tail;</span><br><span class="line">    linked_list-&gt;tail       = new_node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="首删-尾删">首删/尾删</h3>
<p>首删与尾删操作上与首插、尾插相似(甚至更简单)，是后者的逆过程。</p>
<h4 id="首删">首删</h4>
<p>首删主要分为3步进行:</p>
<ol>
<li>更新链表首节点</li>
<li>更新新首节点的<code>xorp</code></li>
<li>释放原首节点内存</li>
</ol>
<p>但执行过程中需要对链表为空或者删除后链表为空的情况进行特殊处理。<br>
具体方式如下:<br>
<em><strong>该函数实现的其实是弹出链表表头的节点，并非单纯的删除</strong></em><br>
时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mn>1</mn><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(1) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord">1</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pop_front</span><span class="params">(Linked_List* linked_list, <span class="type">int</span>* val)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node* head_node = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    head_node = linked_list-&gt;head;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (head_node == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;      </span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    *val = head_node-&gt;value;</span><br><span class="line"></span><br><span class="line">    linked_list-&gt;head = linked_list-&gt;head-&gt;xorp;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        linked_list-&gt;head-&gt;xorp = xor(linked_list-&gt;head-&gt;xorp, head_node);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        linked_list-&gt;tail = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">free</span>(head_node);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  先解释函数：接收linked_list参数用于指定要查找的链表，再接受一个val参数用于接受结果；返回 0 / -1 用于标记是否删除成功。</p>
<p>若链表为空链表，无法进行删除操作，直接返回-1</p>
<p>若链表不为空，则</p>
<ol>
<li>
<p>更新链表首节点为原首节点的后继节点 (<code>linked_list-&gt;head = linked_list-&gt;head-&gt;xorp;</code>)</p>
</li>
<li>
<p>更新首节点的<code>xorp</code>:如果删除后链表变为空链表，无新首节点，则直接将<code>linked_list-&gt;tail</code>设置为<code>NULL</code>。否则，新首节点为原首节点的后继节点，我们这里将原首节点、新首节点和新首节点的后继节点分别设为<code>first</code>、<code>second</code>、和<code>third</code>，则<code>second-&gt;xorp = first XOR third</code>，现在要将其变为<code>NULL XOR third</code>，则仅需将<code>second-&gt;xorp</code>与<code>first</code>进行一次异或运算即可。</p>
</li>
<li>
<p>释放内存：不必多说</p>
</li>
</ol>
<h4 id="尾删">尾删</h4>
<p>时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mn>1</mn><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(1) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord">1</span><span class="mclose">)</span></span></span></span><br>
同理，这里直接放代码:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">pop_back</span><span class="params">(Linked_List* linked_list, <span class="type">int</span>* val)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 尾部删除</span></span><br><span class="line">    Node* tail_node = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;tail == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;      <span class="comment">// Error code</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    tail_node = linked_list-&gt;tail;</span><br><span class="line">    *val      = tail_node-&gt;value;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        tail_node-&gt;xorp 等于 (prev XOR NULL) = prev</span></span><br><span class="line"><span class="comment">        所以新的尾节点是 tail_node-&gt;xorp</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">    linked_list-&gt;tail = linked_list-&gt;tail-&gt;xorp;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;tail != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="comment">// 更新新尾节点的xorp: </span></span><br><span class="line">        <span class="comment">// 原本是 (prev_prev XOR tail_node)，现在应该是 (prev_prev XOR NULL)</span></span><br><span class="line">        <span class="comment">// 所以需要将 tail_node 从 xorp 中移除 (再异或一次 tail_node)</span></span><br><span class="line">        linked_list-&gt;tail-&gt;xorp = xor(linked_list-&gt;tail-&gt;xorp, tail_node);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 链表变为空</span></span><br><span class="line">        linked_list-&gt;head = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">free</span>(tail_node);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="遍历">遍历</h3>
<p>时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mi>n</mi><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(n) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord mathnormal">n</span><span class="mclose">)</span></span></span></span><br>
  设当前节点为<code>cur</code>，其前驱为 <code>pre</code>，后继为<code>nxt</code>。由<code>Node</code>的定义，我们已知当前节点的 <code>xorp</code> 为 <code>pre XOR nxt</code>，那么我们就可以通过 <code>xorp XOR pre</code> 得到 <code>nxt</code>，通过 <code>xorp XOR nxt</code> 得到 <code>pre</code>。只要不断重复计算就实现遍历链表。<br>
具体实现如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">out_linked_list</span><span class="params">(Linked_List* linked_list)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 输出链表</span></span><br><span class="line">    <span class="type">int</span>   cnt = <span class="number">0</span>;</span><br><span class="line">    Node* pre = <span class="literal">NULL</span>;</span><br><span class="line">    Node* cur = <span class="literal">NULL</span>;</span><br><span class="line">    Node* nxt = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur = linked_list-&gt;head;</span><br><span class="line">    nxt = linked_list-&gt;head-&gt;xorp;    <span class="comment">// head-&gt;xorp = NULL ^ next = next</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>(cur != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        ++cnt;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;[%d]:%d\n&quot;</span>,cnt, cur-&gt;value);</span><br><span class="line">        <span class="keyword">if</span> (nxt == <span class="literal">NULL</span>) <span class="keyword">break</span>;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 移动指针到下一个位置</span></span><br><span class="line">        pre = cur;</span><br><span class="line">        cur = nxt;</span><br><span class="line">        nxt = xor(cur-&gt;xorp, pre);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="随机插入-随机删除-随机访问">随机插入/随机删除/随机访问</h3>
<p>  基于遍历和首插/删的原理，随机插入/访问就只需遍历到指定位置然后执行插入或者删除操作即可，需要处理空列表插入/删除以及插入/删除到末尾的特殊情况。</p>
<p>具体实现如下：</p>
<h4 id="随机插入">随机插入</h4>
<p>时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mi>n</mi><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(n) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord mathnormal">n</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">insert</span><span class="params">(Linked_List* linked_list, <span class="type">int</span> value, <span class="type">int</span> pos)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node* pre       = <span class="literal">NULL</span>;</span><br><span class="line">    Node* cur       = <span class="literal">NULL</span>;</span><br><span class="line">    Node* nxt       = <span class="literal">NULL</span>;</span><br><span class="line">    Node* new_node  = <span class="literal">NULL</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 空链表插入</span></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (pos == <span class="number">1</span>) &#123;</span><br><span class="line">            push_back(linked_list, value);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    new_node = create_node(value);</span><br><span class="line">    cur      = linked_list-&gt;head;</span><br><span class="line">    nxt      = linked_list-&gt;head-&gt;xorp;       <span class="comment">// XOR(NULL, next_node) -&gt; next_node</span></span><br><span class="line">    </span><br><span class="line">    pos--;</span><br><span class="line">    <span class="keyword">for</span> (; pos &gt; <span class="number">0</span> &amp;&amp; nxt != <span class="literal">NULL</span>; --pos)&#123;  <span class="comment">// pos == 0 (达到指定位置) 或</span></span><br><span class="line">        pre = cur;                          <span class="comment">// nxt == NULL (达到链表末尾) 时停止</span></span><br><span class="line">        cur = nxt;</span><br><span class="line">        nxt = xor(cur-&gt;xorp, pre);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pos != <span class="number">0</span>) &#123; <span class="comment">// 到达链表末尾且没有达到指定位置--&gt;位置越界</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>; </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    new_node-&gt;xorp = xor(cur, nxt);</span><br><span class="line">    <span class="comment">// cur-&gt;xorp = pre XOR nxt</span></span><br><span class="line">    <span class="comment">// 所以 (cur-&gt;xorp XOR nxt) XOR new_node = pre XOR new_node</span></span><br><span class="line">    cur-&gt;xorp = xor(xor(cur-&gt;xorp, nxt), new_node);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (nxt != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="comment">// nxt-&gt;xorp 原本是 (cur XOR next_next)，现在应该是 (new_node XOR next_next)</span></span><br><span class="line">        <span class="comment">// XOR cur XOR new_code 即可</span></span><br><span class="line">        nxt-&gt;xorp = xor(xor(nxt-&gt;xorp, cur), new_node);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 如果 nxt 为 NULL，说明插入在尾部</span></span><br><span class="line">        linked_list-&gt;tail = new_node;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="随机删除">随机删除</h4>
<p>时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mi>n</mi><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(n) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord mathnormal">n</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">delete</span><span class="params">(Linked_List* linked_list, <span class="type">int</span> pos)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node* pre = <span class="literal">NULL</span>;</span><br><span class="line">    Node* cur = <span class="literal">NULL</span>;</span><br><span class="line">    Node* nxt = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur = linked_list-&gt;head;</span><br><span class="line">    nxt = linked_list-&gt;head-&gt;xorp;</span><br><span class="line">    pos--;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 遍历找到要删除的节点 cur</span></span><br><span class="line">    <span class="keyword">for</span> (; pos &gt; <span class="number">0</span> &amp;&amp; nxt != <span class="literal">NULL</span>; --pos)&#123;</span><br><span class="line">        pre = cur;</span><br><span class="line">        cur = nxt;</span><br><span class="line">        nxt = xor(cur-&gt;xorp, pre);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (pos != <span class="number">0</span>) &#123; <span class="comment">// 到达链表末尾且没有达到指定位置--&gt;位置越界</span></span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果删除的是头节点，更新head</span></span><br><span class="line">    <span class="keyword">if</span> (pre == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        linked_list-&gt;head = nxt;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果删除的是尾节点，更新tail</span></span><br><span class="line">    <span class="keyword">if</span> (nxt == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        linked_list-&gt;tail = pre;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 更新前后节点的异或指针</span></span><br><span class="line">    <span class="comment">// pre-&gt;xorp 原本是 (pre_pre XOR cur)，现在要是 (pre_pre XOR nxt)</span></span><br><span class="line">    <span class="comment">// XOR cur XOR nxt 即可</span></span><br><span class="line">    <span class="keyword">if</span> (pre != <span class="literal">NULL</span>) pre-&gt;xorp = xor(xor(pre-&gt;xorp, cur), nxt);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// nxt-&gt;xorp 原本是 (cur XOR nxt_next)，现在要是 (pre XOR nxt_next)</span></span><br><span class="line">    <span class="comment">// XOR cur XOR pre 即可</span></span><br><span class="line">    <span class="keyword">if</span> (nxt != <span class="literal">NULL</span>) nxt-&gt;xorp = xor(xor(nxt-&gt;xorp, cur), pre);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">free</span>(cur); <span class="comment">// 内存很贵，不要忘记释放</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h4 id="随机访问">随机访问</h4>
<p>时间复杂度 <span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mi>O</mi><mo stretchy="false">(</mo><mi>n</mi><mo stretchy="false">)</mo></mrow><annotation encoding="application/x-tex"> O(n) </annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathnormal" style="margin-right:0.02778em;">O</span><span class="mopen">(</span><span class="mord mathnormal">n</span><span class="mclose">)</span></span></span></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">query</span><span class="params">(Linked_List* linked_list, <span class="type">int</span> pos, <span class="type">int</span>* val)</span></span><br><span class="line">&#123;</span><br><span class="line">    Node* pre = <span class="literal">NULL</span>;</span><br><span class="line">    Node* nxt = <span class="literal">NULL</span>;</span><br><span class="line">    Node* cur = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (linked_list-&gt;head == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cur = linked_list-&gt;head;</span><br><span class="line">    nxt = cur-&gt;xorp;    <span class="comment">// XOR(NULL, next) -&gt; next</span></span><br><span class="line">    pos--;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 遍历链表</span></span><br><span class="line">    <span class="keyword">for</span> (; pos &gt; <span class="number">0</span> &amp;&amp; nxt != <span class="literal">NULL</span>; --pos)&#123;</span><br><span class="line">        pre = cur;</span><br><span class="line">        cur = nxt;</span><br><span class="line">        nxt = xor(cur-&gt;xorp, pre);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (pos != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 确保传过来的不是空指针</span></span><br><span class="line">    <span class="keyword">if</span> (val != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        *val = cur-&gt;value;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  函数解释：接收<code>linked_list</code>参数用于指定要查找的链表，接收<code>pos</code>参数确定要查找的位置 再接受一个<code>val</code>参数用于接受结果；返回 0 / -1 用于标记是否删除成功。</p>
<h3 id="其他">其他</h3>
<h4 id="逆序遍历">逆序遍历</h4>
<p>  只需要把<code>linked_list</code>中的<code>head</code>和<code>tail</code>交换之后再传入函数即可，十分方便。</p>
<h3 id="后记">后记</h3>
<p>  第一篇博文就这样水完了()</p>
<p>  个人感觉异或链表与其他链表最大的区别就是异或链表没有那么强的“链接感”。其他链表的节点或多或少都需要一个指针指向其前驱/后继，而异或链表是通过指针（甚至可以换成<code>unsigned long long</code>等类型）间接存储其前驱和后继的信息，直到需要时才通过计算得到前驱/后继。这使得异或链表更加抽象，遍历时也更加麻烦，需要前驱或者后继的信息才能开始遍历。不过相比其他链表，异或链表的优势也很明显：在实现双向遍历的同时又避免对内存的过多占用，适用于一些内存受限的场景（如嵌入式），同时，理论上，更小的内存占用还能提高缓存命中率以提高程序运行效率，这也略微提高了异或链表的效率。</p>
<p>  读者可以尝试了解完原理后自己从零开始写异或链表的结构和功能，并在其基础上进行拓展，这样或许能更好地体会异或链表的精神。</p>
<p>  这是笔者第一次写文章，若有不足请多多批评指教。</p>
]]></content>
      <categories>
        <category>Study Notes</category>
      </categories>
      <tags>
        <tag>C Lanuage</tag>
        <tag>Data Structure</tag>
      </tags>
  </entry>
  <entry>
    <title>To Start With......</title>
    <url>/posts/47962/</url>
    <content><![CDATA[<p>  尝试搭建个人博客，于是便有了这段文字。未来会在这个博客发布包括但不限于 学习笔记/感悟 的内容。<br>
  受时间和能力限制，没法一次性实现所有理想功能，所以此博客未来会一直处于 搭建中 的状态。<br>
  博客使用<a href="https://hexo.io/zh-cn/">Hexo</a>驱动，主题为<a href="https://theme-next.js.org/">Next</a>。<br>
  以此纪念博客的初次亮相。</p>
<hr>
<p>  2026.1.12 二编<br>
  从头重新搭建了网站，完善了部分功能。</p>
]]></content>
      <categories>
        <category>Others</category>
      </categories>
  </entry>
</search>
